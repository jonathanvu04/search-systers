"""
Ranking utilities built on top of embeddings.

For now this package exposes helpers that operate over mock data so
your teammate can iterate on TF‑IDF logic in isolation from the DB
and frontend wiring.
"""

